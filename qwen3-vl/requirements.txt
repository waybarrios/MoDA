# MoDA + Qwen3-VL-2B
# Tested with Python 3.11, CUDA 12.8

# PyTorch (install separately if you need a specific CUDA version)
# See: https://pytorch.org/get-started/locally/
torch>=2.4.0
torchvision>=0.19.0
torchaudio>=2.4.0

# Hugging Face ecosystem
transformers>=4.57.0
accelerate>=1.11.0
datasets>=4.0.0
peft>=0.17.0
trl>=0.24.0
safetensors>=0.7.0

# Training framework
llamafactory>=0.9.4

# Qwen3-VL specific
qwen-vl-utils>=0.0.14

# Distributed training (optional, for DeepSpeed ZeRO)
deepspeed>=0.16.0

# Evaluation (install lmms-eval from the bundled fork)
# pip install -e lmms-eval/

# Misc
pillow>=11.0.0
einops>=0.8.0
av>=15.0.0
decord>=0.6.0
